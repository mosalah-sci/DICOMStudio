"""Application layer: use cases, workflows and coordination.

This layer is framework-free. It orchestrates domain rules and infrastructure
ports, but never touches widgets, the filesystem or DICOM libraries directly.
Use cases are introduced when the first feature milestone needs them.
"""

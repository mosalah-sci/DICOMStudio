"""Single source of truth for the application version.

Hatchling reads this file for packaging (see ``[tool.hatch.version]`` in
``pyproject.toml``), keeping the packaged and runtime versions identical.
"""

__version__ = "1.0.1"

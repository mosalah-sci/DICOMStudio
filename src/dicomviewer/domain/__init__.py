"""Domain layer: business rules, core entities and exceptions.

This layer must remain independent of any framework (Qt, pydicom, OpenCV,
VTK) and testable in isolation. Infrastructure adapters implement the ports
declared by this layer and by the Application layer.
"""

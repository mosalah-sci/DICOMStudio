"""Presentation layer: Qt windows, widgets, dialogs and view models.

This layer renders state and forwards user interaction. It contains no
business logic, never performs DICOM parsing and never accesses the
filesystem directly.
"""

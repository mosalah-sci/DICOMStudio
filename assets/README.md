# Assets

Repository-only media that is never bundled into the application:

```
assets/
├── branding/    logo source files, design mockups
└── screenshots/ documentation screenshots
```

Runtime assets belong in `resources/`.
